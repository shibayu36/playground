type TodoList @model {
  id: ID!
  name: String!
  items: [Todo!]! @connection(keyName: "byTodoList", fields: ["id"])
}

type Todo @model
  @key(name: "byTodoList", fields: ["todoListId"]) {
  id: ID!
  todoListId: ID!
  name: String!
  description: String
}
